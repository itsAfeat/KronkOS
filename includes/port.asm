; ------------------------------------------------------------------
; get_perm -- Sets the I/O permission
; IN/OUT = Nothing

; ------------------------------------------------------------------
; port_write -- Write to extern port
; IN: DX = port to write to, AL = Byte to write out

; ------------------------------------------------------------------
; port_read -- Read from extern port
; IN: DX = port to read from
; OUT: AL = Byte to write on